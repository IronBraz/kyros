# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/
.pnpm-store/

# Build outputs
dist/
dist-ssr/
build/
.vite/
*.local

# Environment
.env
.env.local
.env.*.local
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS junk
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Claude / AI tooling
.claude/settings.local.json
.claude/.credentials*
.claude/cache/
.aider*

# graphify knowledge graph (regenerated locally via `graphify update .`)
graphify-out/

# OneDrive paranoia
*.tmp
~$*

# TypeScript build info
*.tsbuildinfo

# Project-level exclusions (must never be re-added)
OUTPUT/
Documenti-Analisi/
Backup/
LINKEDIN_*.md
POST_LinkedIn*
*.pptx
_ul
GEMINI.md
.geminiignore

# Owner's private working folder (personal notes, real credentials, scratch files)
_local/

# Knowledge wiki — raw PDFs (processed into markdown pages; originals are local only)
knowledge/_raw/*.pdf

# Claude Code — local permission overrides (machine-specific, not project config)
.claude/settings.local.json
